<!DOCTYPE html>
<html>
<head>
  <title>AppTree</title>
  <%= stylesheet_link_tag    'application', media: 'all'%>
  <%= javascript_include_tag 'application'%>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="modal fade" id="modal-background" tabindex="-1" role="dialog" aria-labelledby="signup-signin" aria-hidden="true">
    <div id="attach-modal" class="modal-dialog modal-lg"></div>
  </div>

  <div class="site-outer-container" id="home">
    <div class="max-container">
      <%= render 'layouts/navbar' %>
      <%= yield %>
    </div>
  </div>

</body>
</html>
